// Se um dos parametros não for passado ele vai ser zero 0
// para isso é nessário fazer com que os parâemtros recenam zero, como no exemplo abaixo.
function soma(n1=0, n2=0) { 
    return n1 + n2
}

console.log(soma(2, 2))